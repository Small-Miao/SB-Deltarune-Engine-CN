//if you step on a checkpoint, die and press continue you will respawn at the checkpoint if you reload
//if you quit the game and come back you will NOT respawn at the checkpoint
//all checkpoints are reset when you save

i=string(op.une)+"="+string(room)+"checkpoint"; ++op.une;